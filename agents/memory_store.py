"""记忆存储模块（占位符）。

当前实现始终返回空字符串，不提供任何历史记忆。
后续可接入向量检索、用户档案数据库、对话摘要等来源，
只需替换 get_memory() 的实现即可，调用方无需改动。
"""

from __future__ import annotations


def get_memory() -> str:
    """返回当前会话的相关历史记忆内容。

    占位符实现：始终返回空字符串。
    启用记忆功能后，此处应返回与当前用户或对话相关的结构化文本，
    例如：
        "用户上次咨询时提到家中有两只猫，老猫名叫橘子，新猫名叫雪球。
         上次诊断结论：两猫初期相处紧张，建议渐进式引入。"
    """
    return ""
